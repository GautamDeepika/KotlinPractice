/*    INTEGER (Byte, Short, Int, lONG)
      FLOATING POINT(Float, Double)
      BOOLEAN(True, False)
      CHARACTER(Char, String)
*/

fun main() {
    var value = 5
    // score = 5.0 can't take decimal value if you want to assign decimal value then you have to type cast it.
    //Type checking feature
    println(value)

    var score: Int = 12
    var temperature : Double = 89.4
    var isRaining: Boolean = false
    var alphabet: Char = 'A'
    var message: String = "Hello World!"
}
