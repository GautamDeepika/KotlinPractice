fun main() {
    var score = 5 //STATICALLY TYPED VARIABLES
    score = 10
    println(score)

    val msg = "Hello World!"
    // msg = "Not allowed"  --Compile time error -- val can't be reassigned.
    println(msg)
}